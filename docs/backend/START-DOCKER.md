# Start Docker Desktop - Quick Guide

## macOS

1. **Cmd+Space** → gõ "Docker" → **Enter**
2. Đợi icon Docker trên menu bar hiển thị (running)
3. Verify: `docker ps` (should show table, not error)

## Sau khi Docker chạy

Chạy lệnh deployment:
```bash
cd /Users/anhnlq/Documents/GitHub/SDLC-Orchestrator
bash scripts/deploy-all-services.sh
```
